## Packages
recharts | For dashboard analytics charts (if needed later) or inventory trends
framer-motion | For smooth page transitions and sidebar animations
date-fns | For formatting dates in tables and forms
react-hook-form | For complex form management
zod | Schema validation
@hookform/resolvers | Connect Zod to React Hook Form
lucide-react | Icons (already in stack but confirming usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
